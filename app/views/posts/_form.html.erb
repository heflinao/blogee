<%= form_for @post do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
  Author: <%= @post.user.username %>
	<%= f.label :content %>
	<%= f.text_field :content %>
	<%= f.submit 'Save' %>
<% end %>
