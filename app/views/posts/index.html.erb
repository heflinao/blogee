<%= link_to 'New Post', new_post_path %>

<h2>Projects</h2>
<ul>
	<% @posts.each do |post| %>
		<li><%= link_to post.title, post_path(post) %></li>
		<li><%= post.author %></li>
		<p><%= post.content %></p>
	<% end %>
</ul>
